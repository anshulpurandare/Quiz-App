# Dependencies
# These are installed by the server during deployment, so we don't need them in the repo.
/node_modules
/frontend/node_modules
/backend/node_modules

# Environment Variables
# These should NEVER be committed. You will set them in the Vercel/Render UI.
.env
/frontend/.env
/backend/.env

# Build artifacts
# Vercel creates this automatically during its build process.
/frontend/build

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System files
.DS_Store
Thumbs.db
